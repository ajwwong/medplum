import { Title, Text } from '@mantine/core';
import { Document } from '@medplum/react';

export function StartSessionPage(): JSX.Element {
  return (
    <Document>
      <Title>Start Session</Title>
      <Text>FREUD IS HERE</Text>
    </Document>
  );
}